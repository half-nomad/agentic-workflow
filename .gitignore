.DS_Store
*.log
node_modules/

# Runtime state (created during execution)
.agentic/

# Local settings (user-specific)
.claude/settings.local.json

# User-owned rule file — belongs in ~/.claude/rules/, never in this repo.
# Guard against accidentally authoring it here and committing personal content.
rules/personal.md

# Logs directory
logs/

# Browser automation artifacts (claude-in-chrome / playwright MCP)
.playwright-mcp/

# Generated image assets (codex-image skill §4.4 default destination)
outputs/
